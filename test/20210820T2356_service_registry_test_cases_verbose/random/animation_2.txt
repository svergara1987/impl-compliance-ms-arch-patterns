/* Initialisation */
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {}
 & services = {}
 & endpoints = {}
 & last_endpoint_query_result = {}
*/
query_health_apis(E3,{});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {}
 & services = {}
 & endpoints = {}
 & last_endpoint_query_result = {}
*/
register(S1);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {}
 & services = {S1}
 & endpoints = {(S1|->{})}
 & last_endpoint_query_result = {}
*/
query_health_apis(E2,{});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {}
 & services = {S1}
 & endpoints = {(S1|->{})}
 & last_endpoint_query_result = {}
*/
register(S3);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {}
 & services = {S1,S3}
 & endpoints = {(S1|->{}),(S3|->{})}
 & last_endpoint_query_result = {}
*/
query_health_apis(E3,{});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {}
 & services = {S1,S3}
 & endpoints = {(S1|->{}),(S3|->{})}
 & last_endpoint_query_result = {}
*/
add_endpoint(E2,S1,HCA2);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E2|->HCA2)}
 & services = {S1,S3}
 & endpoints = {(S1|->{E2}),(S3|->{})}
 & last_endpoint_query_result = {}
*/
query_endpoints(S3,{});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E2|->HCA2)}
 & services = {S1,S3}
 & endpoints = {(S1|->{E2}),(S3|->{})}
 & last_endpoint_query_result = {}
*/
register(S5);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E2|->HCA2)}
 & services = {S1,S3,S5}
 & endpoints = {(S1|->{E2}),(S3|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
register(S4);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E2|->HCA2)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E2}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
add_endpoint(E3,S1,HCA4);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E2|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E2,E3}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
query_endpoints(S4,{});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E2|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E2,E3}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
add_endpoint(E1,S1,HCA2);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E2|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E1,E2,E3}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
remove_endpoint(E1,S1);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E2|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E2,E3}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
add_endpoint(E1,S1,HCA2);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E2|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E1,E2,E3}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
health_check({(E1|->FALSE),(E2|->FALSE),(E3|->FALSE)});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {E1,E2,E3}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E2|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E1,E2,E3}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
query_endpoints(S4,{});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {E1,E2,E3}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E2|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E1,E2,E3}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
health_check({(E1|->FALSE),(E2|->TRUE),(E3|->TRUE)});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {E1}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E2|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E1,E2,E3}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
remove_endpoint(E2,S1);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {E1}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S4,S5}
 & endpoints = {(S1|->{E1,E3}),(S3|->{}),(S4|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
unregister(S4);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {E1}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S5}
 & endpoints = {(S1|->{E1,E3}),(S3|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
query_endpoints(S5,{});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {E1}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S5}
 & endpoints = {(S1|->{E1,E3}),(S3|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
health_check({(E1|->TRUE),(E3|->TRUE)});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4)}
 & services = {S1,S3,S5}
 & endpoints = {(S1|->{E1,E3}),(S3|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
add_endpoint(E4,S1,HCA4);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4)}
 & services = {S1,S3,S5}
 & endpoints = {(S1|->{E1,E3,E4}),(S3|->{}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
unregister(S3);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4)}
 & services = {S1,S5}
 & endpoints = {(S1|->{E1,E3,E4}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
unregister(S5);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4)}
 & services = {S1}
 & endpoints = {(S1|->{E1,E3,E4})}
 & last_endpoint_query_result = {}
*/
query_endpoints(S2,{});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4)}
 & services = {S1}
 & endpoints = {(S1|->{E1,E3,E4})}
 & last_endpoint_query_result = {}
*/
register(S5);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4)}
 & services = {S1,S5}
 & endpoints = {(S1|->{E1,E3,E4}),(S5|->{})}
 & last_endpoint_query_result = {}
*/
unregister(S5);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4)}
 & services = {S1}
 & endpoints = {(S1|->{E1,E3,E4})}
 & last_endpoint_query_result = {}
*/
add_endpoint(E6,S1,HCA1);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4),(E6|->HCA1)}
 & services = {S1}
 & endpoints = {(S1|->{E1,E3,E4,E6})}
 & last_endpoint_query_result = {}
*/
add_endpoint(E5,S1,HCA1);
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4),(E5|->HCA1),(E6|->HCA1)}
 & services = {S1}
 & endpoints = {(S1|->{E1,E3,E4,E5,E6})}
 & last_endpoint_query_result = {}
*/
query_health_apis(E5,{HCA1});
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {}
 & last_health_query_result = {HCA1}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4),(E5|->HCA1),(E6|->HCA1)}
 & services = {S1}
 & endpoints = {(S1|->{E1,E3,E4,E5,E6})}
 & last_endpoint_query_result = {}
*/
health_check({(E1|->FALSE),(E3|->FALSE),(E4|->TRUE),(E5|->FALSE),(E6|->FALSE)})
/* Variables
   last_endpoint_active_query_result = {}
 & unavailable_endpoints = {E1,E3,E5,E6}
 & last_health_query_result = {HCA1}
 & health_apis = {(E1|->HCA2),(E3|->HCA4),(E4|->HCA4),(E5|->HCA1),(E6|->HCA1)}
 & services = {S1}
 & endpoints = {(S1|->{E1,E3,E4,E5,E6})}
 & last_endpoint_query_result = {}
*/
